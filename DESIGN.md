# CUTROOM 문서 가져오기 디자인

## Visual Theme & Atmosphere

문서 폴더를 콘티 초안으로 옮기는 독립 작업 화면이다. 기존 편집기 위에 전체 화면 대화상자를 열어 편집 중인 값을 유지한다. 문서 확인 → 연결·설정 → 생성·불러오기 순서로 진행한다.

설계 가정: 사용자가 색상·서체·밀도를 지정하지 않아 기존 CUTROOM의 주황 강조색과 한국어 입력 작업을 기준으로 `palette=light_clean`, `accent=coral`, `typography=system_ui`, `display=same_as_body`, `layout=two_column`, `mood=professional_minimal`, `density=balanced`, `exclude=animations,gradients,stock_photos,carousel`을 선택했다. 외부 서체·장식 이미지·추가 UI 라이브러리는 필요하지 않다.

## Color Palette & Roles

| 역할 | 토큰 |
|---|---|
| 배경 | #FFFFFF |
| 구획 표면 | #F8FAFC |
| 본문 | #0F172A |
| 보조 본문 | #64748B |
| 강조 | #F97316 |
| 강조 hover | #EA580C |

강조색 위에는 본문색을 사용해 작은 버튼 글자의 대비를 확보한다. 오류·완료는 색과 함께 명시적인 문구로 구분한다.

## Typography Rules

본문은 system-ui, 400, 1rem/1.6이다. 제목은 같은 서체의 600 두께를 사용한다. 경로와 ID만 시스템 고정폭 글꼴로 표시한다. 한국어 설명과 입력값을 작은 영문 레이블 크기로 축소하지 않는다.

## Component Stylings

상단에는 작업 제목과 닫기, 그 아래에는 번호와 단계명이 있는 진행 표시를 둔다. 입력 필드는 전체 테두리와 상시 레이블을 사용한다. 필수 항목은 별표 대신 오류 메시지와 남은 항목으로 설명한다. 주요 버튼 하나에만 강조색을 사용한다. 자동 연결은 접을 수 있는 목록, 미해결 연결은 직접 편집 가능한 행으로 표시한다. 빈 그룹은 렌더링하지 않는다.

## Layout Principles

최대 폭 1200px, 본문과 진행 요약의 두 열이다. 구획 간격 기준은 72px, 콘텐츠 패딩은 24px/40px이다. 문서 폼 전체를 하나의 영역에서 스크롤하며 각 목록에 별도 스크롤을 만들지 않는다. 경로·원문·오류는 줄바꿈한다. 하단 동작 영역에는 다음 단계와 남은 결정을 보여 준다.

## Depth & Elevation

그림자를 사용하지 않는다. 본문색을 낮은 불투명도로 적용한 1px 테두리와 표면색으로 구획을 구분한다. 대화상자는 브라우저의 modal top layer에 표시한다.

## Do's and Don'ts

- 원본에서 확인된 인물 역할·장면 출연 ID·출처만 표시한다. 이름과 ID를 배열 순서로 연결하지 않는다.
- 제작 설정은 사용자가 명시하며 다른 작품의 설정을 복사하지 않는다.
- 서버 검토·생성·불러오기 상태를 각각 표시한다. 실패하면 해당 단계에서 수정할 수 있다.
- 같은 프로젝트 ID가 이미 있으면 새 패키지 생성과 기존 콘티 변경을 구분한다. 기존 콘티를 덮어쓰지 않는다.
- 기존 편집기의 차단 배너와 타임라인을 가져오기 화면에 노출하지 않는다.
- 닫았다 다시 열면 입력과 단계를 유지한다. 페이지 새로고침 이후까지 보존한다고 주장하지 않는다.

## Responsive Behavior

640px, 768px, 1024px, 1280px를 기준으로 폭을 검토한다. 작은 화면은 한 열로 쌓고 요약은 본문 아래로 이동한다. 버튼은 줄바꿈하며 입력 최소 폭 때문에 가로 스크롤이 생기지 않게 한다. 폼 포커스가 하단 동작 영역에 가려지지 않게 한다.

## Agent Prompt Guide

이 디자인의 적용 범위는 문서 가져오기와 진입 버튼이다. 편집기 전체를 재설계하지 않는다. 키보드 포커스, Escape 닫기, 진입 버튼으로 포커스 복귀를 유지한다. 진행 중 중복 요청을 막고 결과 생성 이후 수정으로 기존 성공 경로가 사라지지 않게 한다. 실제 검증 기준과 데이터 계약은 [문서 패키지 Design](docs/02-design/features/document-handoff.design.md)을 따른다.
